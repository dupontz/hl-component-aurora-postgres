test_metadata:
  type: config
  name: database_parameters
  description: set the description for your test

family: aurora-postgresql12
engine: 12.10

cluster_parameters:
  ssl_min_protocol_version: TLSv1.2

instance_parameters:
  rds.force_admin_logging_level: info